# mono-ai-budget-bot

Telegram-бот, який буде підключатися до Monobank Personal API, аналізувати витрати по всіх рахунках та генерувати фінансові звіти з AI-інсайтами.

> 🚧 Поточний етап: ініціалізація репозиторію та CLI smoke test.

---

## 📌 Мета проєкту

Фінальний продукт буде:

- Підключатися до Monobank Personal API
- Отримувати транзакції по всіх рахунках
- Обчислювати метрики витрат за обрані періоди
- Порівнювати періоди (тиждень до тижня, місяць до місяця)
- Генерувати AI-інсайти та рекомендації
- Надсилати звіти через Telegram-бота

На поточному етапі реалізовано лише структуру проєкту та базовий CLI.

---

## 🏗 Структура проєкту

mono-ai-budget-bot/
  src/
    mono_ai_budget_bot/
      __init__.py
      __main__.py
      cli.py
  tests/
  .gitignore
  .env.example
  README.md
  pyproject.toml

---

## ⚙️ Вимоги

- Python 3.11+
- Poetry

Перевірити версію Python:

python --version

---

## 🚀 Встановлення

1) Встановити залежності:

poetry install

2) Запустити smoke test:

poetry run python -m mono_ai_budget_bot health

Очікуваний результат:

ok

3) Перевірити версію:

poetry run python -m mono_ai_budget_bot --version

Очікуваний результат:

0.1.0

---

## 🔐 Безпека

Секретні ключі (Telegram, Monobank, OpenAI) зберігаються у файлі `.env`.

Файл `.env` додано до `.gitignore` і не повинен комітитися в репозиторій.

Для прикладу використовуйте `.env.example`.

---

## 🧪 Що реалізовано на цьому етапі?

- Базова структура проєкту (src layout)
- Конфігурація Poetry
- Мінімальний CLI entrypoint
- Версіонування пакета
- Команда перевірки працездатності (`health`)

---

## ✅ Що потрібно перевірити зараз

1) Залежності встановлюються без помилок:

poetry install

2) CLI працює:

poetry run python -m mono_ai_budget_bot health

3) Версія виводиться коректно:

poetry run python -m mono_ai_budget_bot --version

4) `.env` не відслідковується git:

git status

Файл `.env` НЕ повинен з’являтися у staged або untracked файлах.

---

## 📅 Наступні кроки

- Додати конфігураційний модуль та логування
- Реалізувати клієнт Monobank API
- Додати модуль аналітики
- Інтегрувати Telegram-бота
- Додати інтеграцію з OpenAI